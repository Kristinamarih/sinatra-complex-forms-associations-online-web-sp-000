<h1>Update Pet</h1>

<h2> Edits for <%= @pet.name %></h2>

  <form action="/pets/<%= @pet.id %>" method="POST">
    <label>Edit the Pet's Name:</label>
    <br>
    <input type="text" name="pet[name]" id="pet_name" value="<%= @pet.name %>">
    
    <br>
    <p>Selet an existing owner or create a new one below.</p>
    
    <h3>Existing Owners</h3>
    <% Owner.all.each do |owner| %>
<<<<<<< HEAD
    <input type="radio" name="pet[owner_ids][]" id="<%= owner.id %>" value="<%= owner.id %>" <%= 'checked' if owner.pets.include?(@pet) %>><%= owner.name %></input><br>
=======
    <input type="radio" name="pet[owner_id]" id="<%= owner.id %>" value="<%= owner.id %>" <%= 'checked' if owner.pets.include?(@pet) %>><%= owner.name %></input><br>
>>>>>>> c0c46f18d2559fbda1a4e5b136b5f5aabeaa870c
    <% end %>
    <br>
    
    <h3>New Owner</h3>
    <label>Owner Name:</label>
    <br>
    <input type="text" name="owner[name]" id="owner_name"></input>
    <br><br>
    
    <input type="submit" value="Update Pet">
  </form>

